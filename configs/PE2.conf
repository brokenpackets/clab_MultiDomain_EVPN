! Command: show running-config
! device: PE2 (cEOSLab, EOS-4.34.1F-42424637.4341F (engineering build))
!
no aaa root
aaa authentication policy local allow-nopassword-remote-login
!
username admin privilege 15 role network-admin nopassword
!
no service interface inactive port-id allocation disabled
!
transceiver qsfp default-mode 4x10G
!
service routing protocols model multi-agent
!
hostname PE2
!
spanning-tree mode mstp
no spanning-tree vlan-id 4093-4094
spanning-tree mst 0 priority 4096
!
system l1
   unsupported speed action error
   unsupported error-correction action error
!
vrf instance OVERLAY1
!
interface Ethernet1
   no switchport
   ip address 10.10.1.12/31
   isis enable 1
   isis circuit-type level-2
   isis metric 100
   isis hello padding
   isis network point-to-point
!
interface Ethernet2
   no switchport
   vrf OVERLAY1
   ip address 10.10.1.1/31
!
interface Loopback0
   ip address 10.255.255.1/32
   node-segment ipv4 index 1
   isis enable 1
   isis passive
!
interface Loopback194
   vrf OVERLAY1
   ip address 192.0.2.3/32
!
interface Management0
   ip address 172.20.20.21/24
   ipv6 address 2001:172:20:20::15/64
!
ip routing
ip routing vrf OVERLAY1
!
mpls ip
!
route-map SET_LP permit 10
   set local-preference 1200
!
router bgp 65100
   bgp bestpath skip next-hop igp-cost
   bgp bestpath skip source-vrf
   neighbor OVERLAY_MPLS peer group
   neighbor OVERLAY_MPLS remote-as 65100
   neighbor OVERLAY_MPLS next-hop-self
   neighbor OVERLAY_MPLS update-source Loopback0
   neighbor OVERLAY_MPLS send-community
   neighbor 10.255.255.3 peer group OVERLAY_MPLS
   !
   address-family ipv4
      no neighbor OVERLAY_MPLS activate
      network 10.255.255.1/32
   !
   address-family vpn-ipv4
      neighbor OVERLAY_MPLS activate
      neighbor OVERLAY_MPLS rcf out as_path_replace_65100()
      neighbor default encapsulation mpls next-hop-self source-interface Loopback0
      vpn client ibgp attribute tunneling
   !
   vrf OVERLAY1
      rd 10.255.255.1:15000
      route-target import vpn-ipv4 15000:15000
      route-target export vpn-ipv4 15000:15000
      bgp bestpath skip next-hop igp-cost
      neighbor 10.10.1.0 remote-as 65100
      neighbor 10.10.1.0 route-reflector-client
      neighbor 10.10.1.0 send-community standard
      !
      address-family ipv4
         neighbor 10.10.1.0 activate
         network 192.0.2.3/32
!
router general
   control-functions
      code unit testing
      function as_path_replace_65100 () {
        as_path.remove( { 65100 } );
        return true;
      }
      EOF
   router-id ipv4 10.255.255.1
!
router isis 1
   net 49.1111.0010.0255.0001.00
   is-type level-2
   log-adjacency-changes
   !
   address-family ipv4 unicast
      maximum-paths 4
      fast-reroute ti-lfa mode node-protection
   !
   segment-routing mpls
      no shutdown
!
router multicast
   ipv4
      software-forwarding kernel
   !
   ipv6
      software-forwarding kernel
!
end